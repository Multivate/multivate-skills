# Server-side only: FastAPI origin for Next.js route handlers (BFF → backend).
# Development: defaults to http://127.0.0.1:8000 when unset.
# Production (Vercel): optional if hardcoded in src/lib/internal-api.ts
# INTERNAL_API_URL=https://web-application-multivate-ulv3.onrender.com
