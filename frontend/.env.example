# Server-side only: FastAPI origin for Next.js route handlers (BFF → backend).
# Development: defaults to http://127.0.0.1:8000 when unset.
# Production (Vercel): optional override; if unset, uses https://multivate-api.onrender.com
# INTERNAL_API_URL=http://127.0.0.1:8000
