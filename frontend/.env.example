# Server-side only: FastAPI origin for Next.js route handlers (BFF → backend).
# Development: defaults to http://127.0.0.1:8000 when unset.
# Production (Vercel): REQUIRED — your Render web service URL (no trailing slash), e.g.:
# INTERNAL_API_URL=https://your-service-name.onrender.com
