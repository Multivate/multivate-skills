"use client";

/**
 * Header cart was removed from the product UI. This stub stays so Turbopack / HMR
 * cannot reference a missing symbol if an old chunk still expects `SiteHeaderCart`.
 * It renders nothing.
 */
export function SiteHeaderCart() {
  return null;
}
