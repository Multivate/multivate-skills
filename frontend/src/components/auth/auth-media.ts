/** Hero imagery for auth split panels. */
export const AUTH_IMAGES = {
  /** Sign-up / register — local asset in `public/` */
  registerHero: "/footer-image.png",
  /** Sign-in — local asset in `public/` (filename contains a space) */
  loginHero: "/OIP%20(26).webp",
} as const;
